import { StyleSheet, TouchableOpacity } from "react-native";
//import { unleashChaos } from "aporratoda"

export const styles = StyleSheet.create({
    container:{},
    buttonText:{},
    button:{},
    text:{
        color: "#fff"
    },
    TouchableOpacity:{
        backgroundColor: "black"
    },
    div: {
        borderColor: "black",
        borderRadius: 20,
        borderWidth: 2,
        width: 400,
        height: 700
    }
})